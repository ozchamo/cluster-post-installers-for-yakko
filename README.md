This repository contains post-install tasks for batch runs after YAKKO builds of OpenShift clusters. They don't necessarily depend on YAKKO, so use at your leisure.

Each 'project has its own README.
